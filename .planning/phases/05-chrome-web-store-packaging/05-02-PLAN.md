---
phase: 05
plan: 02
title: Screenshot Editor & Asset Generator
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/screenshot-editor.html
requirements: STORE-05
---

# Phase 05, Plan 02: Screenshot Editor & Asset Generator

<must_haves>
- A standalone HTML file capable of loading, composing, and exporting screenshots to PNG.
- Presets for 1280x800, 440x280, and 1400x560 dimensions.
</must_haves>

## Tasks

<task>
  <read_first>
    - .planning/phases/05-chrome-web-store-packaging/05-CONTEXT.md
    - .planning/phases/05-chrome-web-store-packaging/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `scripts/screenshot-editor.html` exists and contains HTML/JS to render a canvas.
    - Includes presets for 1280x800, 440x280, and 1400x560.
    - Export button triggers a PNG download via `canvas.toBlob()`.
  </acceptance_criteria>
  <action>
    D-08: Support consuming source screenshots from `screenshots/` directory.
    D-09: Create `scripts/screenshot-editor.html` as a standalone client-side tool with customizable headlines/subtext, dark/accent gradients, rounded shadow device frames, and direct PNG export via HTML Canvas and `URL.createObjectURL`.
    D-10: Provide one-click asset dimension presets for 1280x800 (CWS Store Screenshots), 440x280 (Small Promo Tile), and 1400x560 (Marquee Promo Banner).
  </action>
  <verify>
    <automated>bunx prettier --check scripts/screenshot-editor.html</automated>
    <fails_when>File contains malformed HTML or JS syntax errors.</fails_when>
  </verify>
</task>

## Artifacts this phase produces
- `scripts/screenshot-editor.html` (file)
