---
phase: 05
plan: 03
title: Packaging & Submission Automation
wave: 3
depends_on: ["01", "02"]
files_modified:
  - package.json
  - scripts/package-cws.ts
requirements: STORE-01, STORE-02, STORE-05
---

# Phase 05, Plan 03: Packaging & Submission Automation

<must_haves>
- A `package-cws.ts` script that builds, audits, and generates the zip package.
- Automated zip integrity invariant checking.
- Integration into `package.json` as `package:cws`.
</must_haves>

## Tasks

<task>
  <read_first>
    - package.json
    - scripts/audit-build.mjs
    - .planning/phases/05-chrome-web-store-packaging/05-CONTEXT.md
    - .planning/phases/05-chrome-web-store-packaging/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `scripts/package-cws.ts` exists and executes the pipeline: `bun run compile`, `bun test`, `bun run build`, `bun scripts/audit-build.mjs`, and `bunx wxt zip`.
    - Script unzips the result and verifies zero `.ts`, `.tsx`, `.map`, or `.git` files, and checks the version matches `package.json`.
  </acceptance_criteria>
  <action>
    D-11: Create `scripts/package-cws.ts` executing the complete packaging and validation pipeline.
    D-12: Verify zip integrity invariants (no TS sources, no sourcemaps, no test files/git artifacts, exact storage-only permissions, total size < 10MB).
    D-13: Retain version 0.1.0 for initial public CWS submission.
    D-14: Complement packaging automation with submission guide verification.
  </action>
  <verify>
    <automated>bun run compile</automated>
    <fails_when>The TypeScript file `scripts/package-cws.ts` has type errors.</fails_when>
  </verify>
</task>

<task>
  <read_first>
    - package.json
  </read_first>
  <acceptance_criteria>
    - `package.json` contains `"package:cws": "bun run scripts/package-cws.ts"` in the `scripts` block.
  </acceptance_criteria>
  <action>
    D-11: Modify `package.json` to register the `package:cws` script executing `bun run scripts/package-cws.ts`.
  </action>
  <verify>
    <automated>bun run package:cws --help || true</automated>
    <fails_when>The script command cannot be resolved in package.json.</fails_when>
  </verify>
</task>

## Artifacts this phase produces
- `scripts/package-cws.ts` (file)
- `package:cws` (CLI flag)
