---
phase: 04-twemoji-reactions
plan: 05
type: auto
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - e2e/fixtures/x-home.html
  - e2e/reactions.spec.ts
autonomous: true
requirements: [REACT-01, REACT-02, REACT-03, REACT-04, REACT-05, REACT-06]

must_haves:
  truths:
    - "Hovering Like button for 350ms reveals the reaction palette inside #bt-reactions-root with 6 emoji slots (REACT-01, D-01, D-09)"
    - "Holding Like button for 500ms reveals the palette and suppresses native Like click (REACT-02, D-02)"
    - "A normal click on Like executes native Like without opening palette (D-04)"
    - "Clicking a reaction emoji opens native composer and prefills emoji with trailing space, placing caret at end (REACT-03, D-05, D-06, D-07)"
    - "The extension never clicks the submit button, never posts, and never likes on the user's behalf (REACT-04)"
    - "Scrolling the window immediately dismisses the reaction palette (D-03)"
    - "User can customize slots and switch visual styles in popup, seeing live rendering updates (REACT-05, REACT-06, D-10, D-11, D-12)"
    - "Standing build gate passes: zero TypeScript errors, all unit tests pass, and audit-build.mjs 5 assertions pass (REACT-06)"
  artifacts:
    - path: "e2e/fixtures/x-home.html"
      provides: "Mock home timeline with Like buttons, Reply button, and DraftJS modal dialog"
    - path: "e2e/reactions.spec.ts"
      provides: "Playwright E2E test suite covering hover, hold, prefill, scroll dismiss, and popup settings"
  key_links:
    - from: "e2e/reactions.spec.ts"
      to: "e2e/fixtures/x-home.html"
      via: "navigates to fixture and interacts with Like and Reply elements"
      pattern: "x-home.html"
    - from: "e2e/reactions.spec.ts"
      to: "features/reactions/palette-controller.ts"
      via: "queries #bt-reactions-root and shadow DOM slots"
      pattern: "#bt-reactions-root"
  backstop:
    - statement: "Full Playwright E2E suite passes cleanly against unpacked extension in real Chromium context"
      verification: "automated"
    - statement: "Production build bundle satisfies all 5 security assertions in scripts/audit-build.mjs"
      verification: "automated"
---

<objective>
Deliver the Playwright E2E Test Suite & Full Standing Verification Gate for Phase 4:
1. Enhance e2e/fixtures/x-home.html with mock Like buttons ([data-testid="like"] and [data-testid="unlike"]), native Reply button ([data-testid="reply"]), mock DraftJS reply modal dialog ([role="dialog"] [data-testid="tweetTextarea_0"] with contenteditable="true"), and disabled native submit button ([data-testid="tweetButton"]).
2. Author comprehensive Playwright E2E test suite in e2e/reactions.spec.ts testing unpacked extension inside real Chromium context:
   - 350ms hover trigger reveals #bt-reactions-root shadow root containing 6 reaction slots (REACT-01, D-01, D-09, D-14).
   - 500ms long-press hold triggers palette and suppresses native Like click (REACT-02, D-02).
   - Normal single click on Like executes native Like and dismisses open palette (D-04).
   - Clicking reaction emoji prefills reply composer with emoji and trailing space, collapses caret to end, and never auto-submits (REACT-03, REACT-04, D-05, D-06, D-07).
   - Window/feed scroll immediately dismisses palette (D-03).
   - Popup visual style switching updates rendered emoji mode (Normal font vs Twemoji SVG vs Animated Noto WebP) (D-11, REACT-06).
   - Popup slot customizer allows reordering slots and browsing catalog (REACT-05, D-10, D-12).
3. Execute complete standing verification gate: TypeScript typecheck (bun x tsc --noEmit), full Vitest suite (bun run test), production build security audit (bun run verify), and Playwright E2E suite (bun run test:e2e).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-twemoji-reactions/04-CONTEXT.md
@.planning/phases/04-twemoji-reactions/04-RESEARCH.md
@.planning/phases/04-twemoji-reactions/04-PATTERNS.md
@.planning/phases/04-twemoji-reactions/04-UI-SPEC.md
@e2e/fixtures/x-home.html
@e2e/bookmarks.spec.ts
@scripts/audit-build.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E test fixture enhancement with mock reply modal and Like button targets (REACT-01, REACT-02, REACT-03, D-01, D-02, D-05)</name>
  <reversibility rating="reversible">HTML markup additions in e2e/fixtures/x-home.html for local testing only.</reversibility>
  <read_first>
    - .planning/phases/04-twemoji-reactions/04-CONTEXT.md (D-01, D-02, D-05)
    - .planning/phases/04-twemoji-reactions/04-RESEARCH.md (§ Spike 1: 4-Step Insertion Pipeline)
    - e2e/fixtures/x-home.html
  </read_first>
  <files>
    e2e/fixtures/x-home.html
  </files>
  <action>
Update e2e/fixtures/x-home.html to support all Phase 4 E2E interaction scenarios:
1. Ensure tweet articles have explicit action bar buttons:
- Like button: button with data-testid="like" and aria-label="Like" containing SVG heart icon.
- Liked button: button with data-testid="unlike" and aria-label="Liked" for unlike state testing.
- Reply button: button with data-testid="reply" and aria-label="Reply" containing SVG reply icon.
2. Implement mock DraftJS reply modal dialog:
- Include a hidden or dynamically mounted dialog container [role="dialog"] with aria-modal="true".
- Inside dialog, include rich text editable container with data-testid="tweetTextarea_0", role="textbox", contenteditable="true", and spellcheck="true".
- Include native reply submit button with data-testid="tweetButton", aria-disabled="true", and text "Reply".
- Include script helper in fixture so that clicking [data-testid="reply"] unhides/mounts the reply dialog and focuses tweetTextarea_0, accurately mimicking X web client behavior.
- Clicking [data-testid="like"] updates aria-label to "Liked" and toggles data-testid to "unlike" (normal single click behavior per D-04).
  </action>
  <acceptance_criteria>
    - e2e/fixtures/x-home.html contains tweets with [data-testid="like"] and [data-testid="reply"].
    - Clicking reply button reveals mock dialog with [data-testid="tweetTextarea_0"].
    - Mock dialog contains disabled [data-testid="tweetButton"].
    - Markup complies with X DOM structure.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
    <fails_when>build failure</fails_when>
  </verify>
  <done>E2E fixture updated with Like buttons, Reply triggers, and mock DraftJS composer dialog.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E test suite exercising hover/hold, prefill, scroll dismiss, and style switching (REACT-01, REACT-02, REACT-03, REACT-04, REACT-05, REACT-06, D-01..18)</name>
  <reversibility rating="reversible">Additive test file in e2e/reactions.spec.ts.</reversibility>
  <read_first>
    - .planning/phases/04-twemoji-reactions/04-CONTEXT.md (D-01 through D-18)
    - .planning/phases/04-twemoji-reactions/04-RESEARCH.md (§ Validation & Testing Architecture)
    - .planning/phases/04-twemoji-reactions/04-PATTERNS.md (§ 3.21 reactions.spec.ts)
    - e2e/bookmarks.spec.ts
  </read_first>
  <files>
    e2e/reactions.spec.ts
  </files>
  <action>
Create e2e/reactions.spec.ts using Playwright with persistent browser context loading the built Chrome MV3 extension (.output/chrome-mv3):
1. Test 1: Hover trigger and palette rendering (REACT-01, D-01, D-09, D-14, D-15, D-17, D-18):
- Navigate to mock x-home.html fixture.
- Hover over Like button [data-testid="like"]. Wait 400ms (>350ms delay).
- Locate #bt-reactions-root host and access its shadowRoot.
- Assert floating palette container is visible with rounded pill styling.
- Assert exactly 6 reaction slots are rendered (👍, ❤️, 😂, 😮, 😢, 🔥).
- Hover over heart emoji slot (❤️); assert sentiment tooltip appears with text "Love" (D-17).
2. Test 2: Click-and-hold trigger with click suppression (REACT-02, D-02):
- Dispatch pointerdown on Like button. Wait 600ms (>500ms threshold).
- Dispatch pointerup.
- Assert palette opened.
- Assert Like button was NOT activated (data-testid remains "like", aria-label remains "Like" - native click suppressed).
3. Test 3: Normal click executes native Like and dismisses palette (D-04):
- Single click (<200ms) on Like button.
- Assert Like button toggles to liked state.
- Assert reaction palette is closed / not visible.
4. Test 4: Emoji selection and composer prefill (REACT-03, REACT-04, D-05, D-06, D-07):
- Hover Like button, wait 400ms for palette to open.
- Click thumbs-up emoji slot (👍).
- Assert palette immediately closes.
- Assert reply composer [data-testid="tweetTextarea_0"] opened and its content contains "👍 ".
- Assert trailing space exists after emoji and caret is positioned at end (D-07).
- Assert submit button [data-testid="tweetButton"] was NOT clicked programmatically (tweet was NOT sent, REACT-04).
5. Test 5: Immediate dismissal on scroll (D-03):
- Hover Like button to open palette.
- Scroll page window by 100px.
- Assert palette immediately unmounts / vanishes.
6. Test 6: Popup settings and visual style switching (REACT-05, REACT-06, D-10, D-11, D-12):
- Open extension popup page popup.html.
- Click Reactions category tile (D-10).
- Assert ReactionsPanel is displayed with 3 visual style cards and 6 slot customizer rows.
- Select "Normal" style card; switch back to fixture tab and verify palette renders Unicode text spans instead of img tags.
- Select "Animated Noto" style card; switch back to fixture tab and verify palette renders noto-animated/*.webp images.
- Reorder slots in popup customizer; verify order reflects in floating palette (D-12, D-14).
  </action>
  <acceptance_criteria>
    - e2e/reactions.spec.ts contains all 6 Playwright test scenarios.
    - All tests pass against real Chromium context with unpacked extension.
    - bun run test:e2e exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:e2e</automated>
    <fails_when>test failure or timeout</fails_when>
  </verify>
  <done>Playwright E2E test suite authored and passing all interaction and visual style tests.</done>
</task>

<task type="auto">
  <name>Task 3: Full standing build gate execution and ASVS L1 security audit (REACT-04, REACT-06)</name>
  <reversibility rating="reversible">Read-only build and audit verification.</reversibility>
  <read_first>
    - scripts/audit-build.mjs
    - package.json
  </read_first>
  <files>
    scripts/audit-build.mjs
  </files>
  <action>
Execute the full standing verification gate across the entire codebase:
1. Compile check: run `bun x tsc --noEmit` to guarantee 100% clean TypeScript typing with zero errors across all new and modified files.
2. Unit tests: run `bun run test` (vitest run) to verify all unit tests (reactions-storage, reactions-prefiller, reactions-trigger, reactions-catalog, bookmarks, themes, foundation) pass with zero failures.
3. Standing build & security audit: run `bun run verify` which executes `wxt build && node scripts/audit-build.mjs && vitest run`.
Assert that scripts/audit-build.mjs validates all 5 production invariants:
- Assertion 1: permissions exactly ['storage'], zero host_permissions.
- Assertion 2: web_accessible_resources strictly scoped to *://x.com/* and *://twitter.com/*, zero wildcard origins.
- Assertion 3: exactly 1 content script.
- Assertion 4: zero content_security_policy overrides.
- Assertion 5: zero eval() or new Function() calls across all bundled scripts.
  </action>
  <acceptance_criteria>
    - bun x tsc --noEmit exits with code 0.
    - bun run test exits with code 0 (all test suites passing).
    - bun run verify exits with code 0 and outputs [audit-build] PASSED: All 5 assertions passed.
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
    <fails_when>non-zero exit code or audit failure</fails_when>
  </verify>
  <done>Standing build gate and ASVS L1 security audit passed with 100% compliance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright Automation Harness → Chromium Instance | Browser test runner orchestrating real browser process |
| Web Content Script → Host Page Context | Injected extension logic interacting with host DOM |
| Manifest Declaration → Chrome Web Store Verification | Enforced security constraints on permissions and origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Elevation of Privilege | Manifest permission creep | high | mitigate | scripts/audit-build.mjs Assertion 1 strictly enforces permissions: ['storage'] with zero host_permissions; build gate fails if any extra permission is added. |
| T-04-18 | Repudiation | Software-initiated posting (Anti-Abuse) | high | mitigate | Strict prefill-only architecture (REACT-04). E2E test verifies tweet submit button is never clicked; send action remains 100% human-initiated. |
| T-04-19 | Information Disclosure | Wildcard web_accessible_resources | medium | mitigate | scripts/audit-build.mjs Assertion 2 rejects any match containing <all_urls> or *://*/*; twemoji/* and noto-animated/* are scoped strictly to x.com and twitter.com. |
| T-04-20 | Denial of Service | Unhandled rejections during interaction | medium | mitigate | All async prefiller and catalog routines use defensive try-catch blocks with graceful fallbacks. |
</threat_model>

<verification>
- bun x tsc --noEmit
- bun run test
- bun run test:e2e
- bun run verify
</verification>

<success_criteria>
- All 8 must_haves.truths hold.
- e2e/fixtures/x-home.html contains required Like and Reply test targets.
- e2e/reactions.spec.ts executes and passes all 6 E2E scenarios.
- bun run verify succeeds with all 5 audit assertions passing.
</success_criteria>

<output>
Artifacts created/updated:
- e2e/fixtures/x-home.html (fixture updates)
- e2e/reactions.spec.ts (Playwright test suite)
</output>
