---
phase: 03-bookmarks-capture-management-resurfacing
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - entrypoints/background.ts
  - e2e/fixtures/x-bookmarks.html
  - e2e/bookmarks.spec.ts
  - scripts/audit-build.mjs
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03, BOOK-04, BOOK-05, BOOK-06, BOOK-07, BOOK-08, BOOK-09, BOOK-10]

must_haves:
  truths:
    - "Playwright end-to-end test suite executes against built unpacked Chrome MV3 extension loaded into real Chromium context with 100% pass rate (BOOK-01..10)"
    - "In-page bookmarks hub mounting, sticky positioning, search filtering, and folder chip selection are verified on x.com/bookmarks (D-01, D-02, BOOK-06)"
    - "+ New Folder creation via in-page Radix popover creates a new folder chip and persists to local:folders (D-03, BOOK-04)"
    - "Dual-save action bar click triggers native save propagation while mounting folder selector popover and storing normalized tweet (D-05, D-07, D-15)"
    - "Timeline resurfacing card injects into /home feed every N tweets with '📌 Resurfaced from [Folder]' header, theme accent tint, and functional overflow menu (D-11, D-12, D-14, BOOK-07)"
    - "Resurfaced card mounting introduces zero scroll position displacement and preserves layout containment (BOOK-09)"
    - "Extension popup BookmarksPanel renders sync status, cadence slider, folder checklist, quota progress bar, and JSON export/import (D-12, D-13, D-16, D-17)"
    - "All 5 standing security build invariants are verified: permissions strictly ['storage'], zero host_permissions, scoped WAR (x.com/twitter.com), 1 production content script, zero CSP overrides, zero dynamic code execution (STORE-01, STORE-02)"
  artifacts:
    - path: "entrypoints/background.ts"
      provides: "Runtime message listener dispatching background tab creation for bookmark sync"
      contains: "bt:start-sync"
    - path: "e2e/fixtures/x-bookmarks.html"
      provides: "High-fidelity HTML mock of x.com/bookmarks page structure with primaryColumn, sticky header, tweet cells, and action rows"
    - path: "e2e/bookmarks.spec.ts"
      provides: "Playwright E2E test suite covering in-page hub, search filtering, folder management, dual-save, timeline resurfacing, and popup panel"
    - path: "scripts/audit-build.mjs"
      provides: "Standing build audit script enforcing the 5 security invariants across the built extension package"
  key_links:
    - from: "e2e/bookmarks.spec.ts"
      to: ".output/chrome-mv3"
      via: "loads the built extension package into persistent Chromium browser context"
      pattern: "--load-extension="
    - from: "e2e/bookmarks.spec.ts"
      to: "e2e/fixtures/x-bookmarks.html"
      via: "fulfills route https://x.com/i/bookmarks and https://x.com/bookmarks with fixture HTML"
      pattern: "x-bookmarks.html"
    - from: "scripts/audit-build.mjs"
      to: ".output/chrome-mv3/manifest.json"
      via: "asserts permissions, host_permissions, web_accessible_resources, and content_scripts"
      pattern: "Assertion 1 passed"
  backstop:
    - statement: "All unit test suites, TypeScript typecheck, production build, and security audit pass green"
      verification: "bun run verify"
    - statement: "Folder checklist in BtPopover caps at max-height: 240px and chips truncate at 160px"
      verification: "e2e-assertion"
---

<objective>
Execute full system integration, comprehensive Playwright end-to-end testing, and standing build security verification for Phase 3: Bookmarks (Capture, Management & Resurfacing).

Deliverables:
1. High-fidelity HTML fixture (`e2e/fixtures/x-bookmarks.html`) replicating X's native bookmarks page layout, primary column, sticky header, and action buttons.
2. Comprehensive Playwright E2E test suite (`e2e/bookmarks.spec.ts`) validating the entire bookmarks pipeline: in-page hub mount, instant multi-token search filtering, folder creation popover, action row dual-save interception, timeline resurfacing card injection without scroll jumping, and popup BookmarksPanel controls (BOOK-01 through BOOK-10).
3. Verification of all locked user decisions (D-01 through D-18) against the live built extension inside real Chromium.
4. Standing build security audit gate execution (`node scripts/audit-build.mjs`) confirming zero regression on permissions (`['storage']`), zero dynamic execution, and strictly scoped WAR.
5. Clean green exit for `bun run verify`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bookmarks-capture-management-resurfacing/03-CONTEXT.md
@.planning/phases/03-bookmarks-capture-management-resurfacing/03-RESEARCH.md
@.planning/phases/03-bookmarks-capture-management-resurfacing/03-UI-SPEC.md
@scripts/audit-build.mjs
@e2e/clean-timeline.spec.ts
@e2e/fixtures/x-home.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright end-to-end test suite for Phase 3 Bookmarks (BOOK-01 through BOOK-10, D-01 through D-18)</name>
  <reversibility rating="reversible">Test suite and fixture creation; zero mutation of production source code.</reversibility>
  <read_first>
    - .planning/phases/03-bookmarks-capture-management-resurfacing/03-CONTEXT.md (D-01 through D-18)
    - .planning/phases/03-bookmarks-capture-management-resurfacing/03-UI-SPEC.md (§ Copywriting Contract, § Layout & Interaction Contract)
    - e2e/clean-timeline.spec.ts
    - e2e/fixtures/x-home.html
  </read_first>
  <files>
    entrypoints/background.ts,
    e2e/fixtures/x-bookmarks.html,
    e2e/bookmarks.spec.ts
  </files>
  <action>
0. Update entrypoints/background.ts:
   - Add listener in browser.runtime.onMessage for message.type === 'bt:start-sync'.
   - When received, invokes browser.tabs.create({ url: 'https://x.com/i/bookmarks', active: false }) to open a background inactive tab for scraping without stealing focus from active user browsing.

1. Create e2e/fixtures/x-bookmarks.html:
   - Replicates native X bookmarks page layout:
     - Root div with data-testid="primaryColumn".
     - Sticky top header with Bookmarks title and user avatar.
     - Bookmarks timeline feed div with role="region" and aria-label="Timeline: Bookmarks".
     - Multiple synthetic tweet article elements (article[data-testid="tweet"]) containing permalinks (/status/123456789), author headers (data-testid="User-Name"), tweet text (data-testid="tweetText"), and action row (role="group") with button[data-testid="bookmark"] and button[data-testid="removeBookmark"].

2. Implement e2e/bookmarks.spec.ts:
   - Setup: launches persistent Chromium context loading unpacked extension from .output/chrome-mv3 (same pattern as clean-timeline.spec.ts).
   - Routes:
     - Maps https://x.com/i/bookmarks and https://x.com/bookmarks to x-bookmarks.html fixture.
     - Maps https://x.com/home and https://x.com/ to x-home.html fixture.
   - Test 1: In-Page Bookmarks Hub Mounting (BOOK-06, D-01):
     - Navigates to https://x.com/i/bookmarks.
     - Asserts div#bt-bookmarks-hub-root is attached directly beneath native header.
     - Asserts search input is visible with placeholder "Search bookmarks by text or author...".
     - Asserts filter chips row renders "All Bookmarks" chip and "+ New Folder" trigger chip.
   - Test 2: In-Place Feed Filtering & Empty State (BOOK-05, D-02, D-04):
     - Types search query into search input.
     - Asserts non-matching tweet cells receive data-bt-bookmark-filtered="true" and are hidden.
     - Asserts active filter banner displays "Filtered by: [query]" with "Clear Filters" CTA.
     - Types non-existent query (e.g. "xyznonexistentquery"): asserts BookmarksEmptyState card appears with heading "No Bookmarks Found" and "Clear Filters" button.
     - Clicks "Clear Filters": asserts input resets and all tweets reappear.
   - Test 3: Inline Folder Creation & Deletion (BOOK-04, D-03):
     - Clicks "+ New Folder" chip: asserts BtPopover opens with title "Create Folder".
     - Types "Research Notes", selects green color swatch, clicks "Create Folder".
     - Asserts new folder chip "Research Notes" appears with green color dot and count badge.
     - Asserts folder is stored in local:folders in background storage.
   - Test 4: Action Row Dual-Save & Folder Selector Popover (BOOK-01, D-05, D-07, D-10):
     - Navigates to tweet in feed.
     - Clicks button[data-testid="bookmark"].
     - Asserts FolderSelectorPopover opens anchored to button with title "Save to Folders".
     - Asserts "Uncategorized" folder is listed with checked state.
     - Checks "Research Notes": asserts tweet.folderIds includes the new folder ID in local:bookmarks.
     - Asserts button tooltip displays "Saved in Research Notes".
   - Test 5: Synchronized Unbookmarking (D-09):
     - Clicks unbookmark button on bookmarked tweet.
     - Asserts tweet ID is removed from local:bookmarks in storage.
   - Test 6: Timeline Resurfacing on /home (BOOK-07, BOOK-08, BOOK-09, D-11, D-14):
     - Navigates to https://x.com/home.
     - Seeds local:bookmarks with 5 test bookmarks and sets resurfacingInterval: 2.
     - Scrolls timeline down: asserts container with data-bt-resurfaced-cell="true" mounts into feed.
     - Asserts card renders header "📌 Resurfaced from [Folder]" and 3px accent left stripe.
     - Asserts page scroll position does not jump upon card injection (BOOK-09).
   - Test 7: Resurfaced Card Quick-Action Menu (D-11):
     - Clicks overflow menu button (···) on resurfaced card.
     - Asserts menu items: "Snooze for 7 days", "Don't resurface this tweet", "Move to folder...", "Remove from bookmarks".
     - Clicks "Snooze for 7 days": asserts card disappears and snoozedUntil is updated in storage.
   - Test 8: Popup BookmarksPanel Integration (BOOK-08, BOOK-10, D-12, D-16, D-17):
     - Opens extension popup window.
     - Clicks "Bookmarks" tile in grid: asserts BookmarksPanel opens.
     - Asserts "Sync Bookmarks Now" button, cadence slider, folder eligibility list, and quota bar render.
     - Changes cadence slider to 25: asserts bookmarksSettingsItem.resurfacingInterval is 25.

3. Run E2E test suite: bun run test:e2e.
  </action>
  <acceptance_criteria>
    - e2e/fixtures/x-bookmarks.html provides valid DOM structure for x.com bookmarks feed.
    - e2e/bookmarks.spec.ts covers all 8 test scenarios.
    - All tests pass with 100% pass rate in real Chromium context.
    - Zero console errors or uncaught exceptions during test execution.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:e2e</automated>
    <fails_when>non-zero exit or test failures</fails_when>
  </verify>
  <done>Playwright E2E test suite for Phase 3 bookmarks passes 100% green.</done>
</task>

<task type="auto">
  <name>Task 2: Full unit test suite, strict TypeScript typecheck, production build & standing security audit gate (BOOK-10, STORE-01, STORE-02)</name>
  <reversibility rating="reversible">Verification and audit task; verifies production integrity across all project invariants.</reversibility>
  <read_first>
    - scripts/audit-build.mjs
    - .claude/CLAUDE.md (C-1 through C-14)
    - package.json
  </read_first>
  <files>
    scripts/audit-build.mjs
  </files>
  <action>
1. Run all unit tests across the entire repository:
   - bun run test (runs vitest across bookmarks-storage, bookmarks-normalization, bookmarks-search, bookmarks-capture, bookmarks-action-bar, bookmarks-in-page-ui, bookmarks-resurfacing, bookmarks-popup, metrics, clutter, tabs, theme-engine, layout, settings-migration).
   - Verify all tests pass with zero failures.

2. Run strict TypeScript compilation check:
   - bun x tsc --noEmit.
   - Verify zero type errors or unused symbol warnings.

3. Run production extension build:
   - bun run build.
   - Verify build outputs single package into .output/chrome-mv3.

4. Execute standing build security audit gate:
   - node scripts/audit-build.mjs.
   - Verify all 5 security assertions pass:
     - Assertion 1: Minimal permissions: strictly ['storage'], zero host_permissions (STORE-01).
     - Assertion 2: Scoped web accessible resources: strictly x.com / twitter.com.
     - Assertion 3: Exactly 1 production content script.
     - Assertion 4: Zero manifest CSP overrides.
     - Assertion 5: Zero dynamic code execution constructs (eval(), new Function()) in any bundled JS file (STORE-02).

5. Execute unified verification gate:
   - bun run verify.
  </action>
  <acceptance_criteria>
    - bun run test exits 0 with 100% passing tests.
    - bun x tsc --noEmit exits 0 with zero errors.
    - bun run build produces valid .output/chrome-mv3 bundle.
    - node scripts/audit-build.mjs outputs "PASSED: All 5 assertions passed".
    - bun run verify completes successfully.
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Full unit test suite, TypeScript strict check, production build, and standing security audit gate all pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extension Bundle → Chrome Extension Runtime | The built MV3 package loaded into Chromium |
| Web Accessible Resources → Web Origin | Scripts exposed to x.com / twitter.com |
| Manifest Permissions → Chrome Web Store Policy | Minimal privilege compliance (STORE-01, STORE-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Elevation of Privilege | Excessive extension permissions | high | mitigate | audit-build.mjs Assertion 1 verifies permissions is strictly ['storage'] and host_permissions is absent (STORE-01). |
| T-03-18 | Information Disclosure | Unscoped web accessible resources | high | mitigate | audit-build.mjs Assertion 2 verifies web_accessible_resources strictly matches x.com and twitter.com, eliminating cross-site fingerprinting. |
| T-03-19 | Tampering | Dynamic code execution in bundle | critical | mitigate | audit-build.mjs Assertion 5 scans every bundled file for eval and new Function, ensuring zero dynamic execution (STORE-02). |
| T-03-20 | Denial of Service | Content script bloat or multiple injectors | medium | mitigate | audit-build.mjs Assertion 3 enforces exactly one production content script entrypoint. |
</threat_model>

<verification>
- bun run test:e2e
- bun run verify
</verification>

<success_criteria>
- All 8 must_haves.truths hold.
- E2E Playwright test suite passes covering bookmarks capture, in-page hub, search filtering, folder management, dual-save, timeline resurfacing, and popup panel.
- All unit test suites across the repository pass cleanly.
- TypeScript compiler reports zero errors.
- Standing security audit gate passes all 5 assertions with zero violations.
</success_criteria>

## Artifacts this phase produces

**Background & Messaging:**
- `entrypoints/background.ts` — `bt:start-sync` message handler opening inactive background tab

**Test Fixtures & E2E Suites:**
- `e2e/fixtures/x-bookmarks.html` — High-fidelity x.com bookmarks fixture
- `e2e/bookmarks.spec.ts` — Comprehensive Playwright E2E test suite

**Audit & Build Verification:**
- `.output/chrome-mv3` — Production-ready unpacked MV3 extension bundle
- Verified `scripts/audit-build.mjs` execution log
