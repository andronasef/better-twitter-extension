---
phase: 02-clean-timeline-themes
plan: 05
type: execute
wave: 5
depends_on: [02-02, 02-04]
files_modified:
  - e2e/fixtures/x-home.html
  - e2e/theme-switch.spec.ts
  - e2e/clean-timeline.spec.ts
  - e2e/tabs.spec.ts
  - scripts/audit-build.mjs
autonomous: true
requirements: [CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05, THEME-01, THEME-02, THEME-03, THEME-04, THEME-05, THEME-06, THEME-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: high

coupling_justified: []

must_haves:
  truths:
    - "All declutter features (trends removal, who-to-follow removal, vanity metric stripping, tab reordering) pass end-to-end browser verification in Playwright"
    - "All theme presets (Dracula, Nord, Matrix, Minimal, Old Twitter) and custom accent overrides apply live with zero FOUC and survive route transitions in Playwright"
    - "Production extension package builds cleanly into .output/chrome-mv3 and passes all 5 security invariants in scripts/audit-build.mjs (minimal storage permission, zero host permissions, 1 content script, 0 CSP overrides, 0 dynamic code execution)"
    - "All unit test suites (metrics, clutter, tabs, theme-engine, layout, settings-migration) pass with zero errors"
  artifacts:
    - path: "e2e/fixtures/x-home.html"
      provides: "Realistic synthetic fixture mimicking x.com home page with action counts, sidebar modules, and tablist"
      contains: "sidebarColumn"
    - path: "e2e/theme-switch.spec.ts"
      provides: "E2E Playwright tests for zero-FOUC theme switching and custom accent recoloring"
    - path: "e2e/clean-timeline.spec.ts"
      provides: "E2E Playwright tests for vanity metric stripping and sidebar clutter suppression"
    - path: "e2e/tabs.spec.ts"
      provides: "E2E Playwright tests for flex order tab swapping and Following auto-selection"
  key_links:
    - from: "e2e/theme-switch.spec.ts"
      to: ".output/chrome-mv3"
      via: "loads unpacked extension in Chromium context and navigates to route-fulfilled fixture"
      pattern: "chromium\\.launchPersistentContext"
    - from: "scripts/audit-build.mjs"
      to: ".output/chrome-mv3/manifest.json"
      via: "audits permissions and bundled script files"
      pattern: "audit-build"
---

<objective>
Execute Full Phase Integration, E2E Browser Verification, and Standing Build Gate Audits for Phase 2.

Deliverables:
1. End-to-end Playwright tests verifying the live behavior of all Phase 2 features (CLEAN-02..05, THEME-01..07) inside a real Chromium browser context running the built unpacked extension.
2. Updated synthetic HTML test fixture (`e2e/fixtures/x-home.html`) including sidebar trends, who-to-follow modules, vanity metric action bars, detail stats, and home tablist.
3. Successful completion of the full unit test suite, TypeScript compilation, and production build audit gate (`scripts/audit-build.mjs`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/02-clean-timeline-themes/02-CONTEXT.md
@.planning/phases/02-clean-timeline-themes/02-RESEARCH.md
@.planning/phases/02-clean-timeline-themes/02-UI-SPEC.md
@.planning/phases/02-clean-timeline-themes/02-VALIDATION.md
@playwright.config.ts
@scripts/audit-build.mjs
@e2e/fixtures/x-home.html
@e2e/tracer.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end Playwright tests for declutter, themes & layouts (CLEAN-02..05, THEME-01..07)</name>
  <reversibility rating="reversible">Creates and updates test files and fixtures under e2e/; no production code modification.</reversibility>
  <read_first>
    - .planning/phases/02-clean-timeline-themes/02-VALIDATION.md (§ Test Strategies, § Per-Task Verification Map)
    - e2e/tracer.spec.ts
    - e2e/fixtures/x-home.html
    - playwright.config.ts
  </read_first>
  <files>
    e2e/fixtures/x-home.html,
    e2e/clean-timeline.spec.ts,
    e2e/tabs.spec.ts,
    e2e/theme-switch.spec.ts
  </files>
  <action>
1. Update `e2e/fixtures/x-home.html`:
   - Add realistic DOM elements matching live X markup:
     - `[role="tablist"]` with child 1 ("For you") and child 2 ("Following").
     - `div[data-testid="sidebarColumn"]` with:
       - Search input container.
       - Trends module: `section:has([data-testid="trend"])` with `[data-testid="trend"]` items.
       - Who to Follow module: `aside:has([data-testid="UserCell"])` with `[data-testid="UserCell"]` items.
       - Premium upsell: `aside:has(a[href*="/i/premium_sign_up"])`.
     - Tweet cells with `[role="group"]` containing Reply, Repost, Like buttons with `[data-testid="app-text-transition-container"]` count numbers, and an analytics view button `a[href*="/analytics"]`.
     - Tweet detail stats block `article div:has(> a[href$="/retweets"])`.

2. Create `e2e/clean-timeline.spec.ts`:
   - Test vanity metrics stripping:
     - When `hideVanityMetrics` is enabled in settings:
       - Action buttons remain visible and interactive.
       - Count containers (`[data-testid="app-text-transition-container"]`) have computed `display: none`.
       - Analytics button has computed `display: none`.
       - Tweet detail stats row has computed `display: none`.
   - Test sidebar clutter suppression:
     - When `cleanSidebar` is enabled in settings:
       - Trends module, Who to Follow module, and Premium upsell have computed `display: none`.
       - Search input container remains visible.

3. Create `e2e/tabs.spec.ts`:
   - Test tab reordering:
     - When `swapHomeTabs` is enabled:
       - Following tab has computed CSS `order: 1`.
       - For You tab has computed CSS `order: 2`.
     - When `hideForYouTab` is enabled:
       - For You tab has computed `display: none`.
   - Test Following auto-selection on `/home` route navigation.

4. Create `e2e/theme-switch.spec.ts`:
   - Test theme switching:
     - Initial load with Dracula theme: `<html>` has `data-bt-theme="dracula"` synchronously; body background matches `#282a36`.
     - Live switch to Nord: `<html>` updates to `data-bt-theme="nord"` without page reload; background matches `#2e3440`.
     - Live switch to Matrix: background matches `#000000` and text matches `#00ff66`.
     - Custom accent color: setting hex `#ff007f` updates `--bt-theme-accent` on `document.documentElement` and button styles compute to `#ff007f`.
     - Minimal theme: sidebar is hidden, primaryColumn is centered.
     - Old Twitter layout: avatars have `border-radius: 4px`, top navbar is 46px horizontal bar.

5. Run Playwright test suite.
  </action>
  <acceptance_criteria>
    - `e2e/clean-timeline.spec.ts` passes: verifies metric counts and sidebar clutter hiding.
    - `e2e/tabs.spec.ts` passes: verifies flex order tab swap and Following auto-activation.
    - `e2e/theme-switch.spec.ts` passes: verifies live theme changes, zero-FOUC, custom accent recoloring, Minimal, and Old Twitter layouts.
    - `bun run test:e2e` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:e2e</automated>
    <fails_when>non-zero exit or test failures</fails_when>
  </verify>
  <done>E2E Playwright test suite covering all Phase 2 features passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Full unit test suite completion & standing build audit verification</name>
  <reversibility rating="reversible">Build and audit verification; ensures all invariants remain intact.</reversibility>
  <read_first>
    - scripts/audit-build.mjs
    - .claude/CLAUDE.md (C-1..C-14)
    - .planning/phases/02-clean-timeline-themes/02-VALIDATION.md
  </read_first>
  <files>
    scripts/audit-build.mjs
  </files>
  <action>
1. Run all unit tests across the entire codebase:
   - `bun run test` (including `metrics.test.ts`, `clutter.test.ts`, `tabs.test.ts`, `theme-engine.test.ts`, `layout.test.ts`, `settings-migration.test.ts`).

2. Run TypeScript strict typecheck:
   - `bun x tsc --noEmit`.

3. Run production build:
   - `bun run build`.

4. Run standing build audit gate:
   - `node scripts/audit-build.mjs`.
   - Verify all 5 invariants pass:
     - Minimal permissions: strictly `['storage']`, zero `host_permissions`.
     - Scoped web accessible resources: strictly `x.com` / `twitter.com`.
     - Exactly 1 production content script.
     - Zero manifest CSP overrides.
     - Zero dynamic code execution constructs (`eval()`, `new Function()`) in any bundled JS file.
  </action>
  <acceptance_criteria>
    - All unit test suites pass (`bun run test` exits 0).
    - TypeScript compilation completes with zero errors (`bun x tsc --noEmit` exits 0).
    - Production build succeeds into `.output/chrome-mv3`.
    - `node scripts/audit-build.mjs` exits 0 with "PASSED: All 5 assertions passed".
  </acceptance_criteria>
  <verify>
    <automated>bun run test &amp;&amp; bun x tsc --noEmit &amp;&amp; bun run build &amp;&amp; node scripts/audit-build.mjs</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Unit test suite, TypeScript typecheck, production build, and security audit gate all pass green.</done>
</task>

</tasks>
